import type { Config } from 'tailwindcss'

/**
 * TAILWIND CSS CONFIGURATION
 * Customized setup for the CrisisPM platform with design system tokens,
 * content paths for purging unused styles, and theme extensions for branding.
 */
const config: Config = {
  /**
   * CONTENT SCANNING CONFIGURATION
   * Defines which files Tailwind should scan for class names to include in the final CSS.
   * Includes all TypeScript/JavaScript files in src directory and common file extensions.
   */
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],

  /**
   * THEME CUSTOMIZATION
   * Extends default Tailwind theme with CrisisPM-specific design tokens.
   * Colors use HSL format for better accessibility and theming flexibility.
   */
  theme: {
    extend: {
      colors: {
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        card: {
          DEFAULT: 'hsl(var(--card))',
          foreground: 'hsl(var(--card-foreground))',
        },
        popover: {
          DEFAULT: 'hsl(var(--popover))',
          foreground: 'hsl(var(--popover-foreground))',
        },
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        secondary: {
          DEFAULT: 'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))',
        },
        muted: {
          DEFAULT: 'hsl(var(--muted))',
          foreground: 'hsl(var(--muted-foreground))',
        },
        accent: {
          DEFAULT: 'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))',
        },
        destructive: {
          DEFAULT: 'hsl(var(--destructive))',
          foreground: 'hsl(var(--destructive-foreground))',
        },
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
      fontFamily: {
        sans: ['var(--font-sans)', 'system-ui', 'sans-serif'],
      },
    },
  },

  /**
   * PLUGINS CONFIGURATION
   * Additional Tailwind plugins for enhanced functionality.
   * Can be extended with plugins for forms, typography, animations, etc.
   */
  plugins: [],
}

export default config